require "spec_helper"

RSpec.describe Brewer::Stats do
  it "has a version number" do
    expect(Brewer::Stats::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
